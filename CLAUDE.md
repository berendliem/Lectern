# Lectern

Repository conventions live in `AGENTS.md` so every agent reads the same copy.

@AGENTS.md

The rule that gets broken most often, restated here because it is worth reading twice:
**`main` is protected — never commit or push to it directly.** Changes land through a
pull request unless a human tells you otherwise for that specific change.
